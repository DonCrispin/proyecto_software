<%= form_for(@editorial, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @editorial.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@editorial.errors.count, "error") %> prohibió esta editorial de ser guardada:</h4>

      <ul>
      <% @editorial.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="col-lg-15">
      <%= f.text_field :name, class: "form-control", :placeholder => "Nombre..." %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-15">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
